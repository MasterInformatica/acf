*** OJO: Quizas es conveniente declarar los estados iniciales aquí en vez
*** de en ejemplos.

load model-checker
load ejemplo


load props


(omod CHECK is
    pr PROPS .
    pr EJEMPLO .
    pr MODEL-CHECKER .
    pr LTL-SIMPLIFIER .
    
        
endom)
( red modelCheck ( inicio2, existeNodo("1.1.1.1") -> existeNodo("2.2.2.2")) . )

